// src/app/mypage/edit/page.tsx

/**
 * 마이페이지 - 회원정보 수정 페이지

// 상대 경로 대신 절대 경로 사용을 권장 (tsconfig.json의 baseUrl, paths 설정 활용)
import MypageEditComponent from "@/components/MypageEdit";
**/

import MypageEditComponent from "@/components/MypageEdit";

export default function MypageEditPage() {
  return <MypageEditComponent />;
}
