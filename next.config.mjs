/** @type {import('next').NextConfig} */
const nextConfig = {
  // experimental: {
  //   appDir: true, // app 디렉토리 활성화
  // },
  eslint: {
    ignoreDuringBuilds: true,
  },
};

export default nextConfig;
